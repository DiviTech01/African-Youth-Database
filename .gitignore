# Dependencies
node_modules
.pnpm-store

# Build outputs
dist
dist-ssr
build

# Local-disk fallback for R2 uploads (apps/api/uploads/* and ./uploads/*)
uploads/
apps/api/uploads/

# Local-only DB snapshots + one-off migration plans (some are 16+ MB).
# Kept on disk for rollback but don't ship in git history.
apps/api/backups/

# One-off diagnostic scripts (use apps/api/scripts/ for anything reusable).
apps/api/check-db-audit.js

# Environment
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Prisma
packages/database/prisma/migrations
packages/database/node_modules/.prisma

# Editor
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store

# Docker
docker-data/

# OS
Thumbs.db

# Electron build artefacts — keep these out of git
release/
